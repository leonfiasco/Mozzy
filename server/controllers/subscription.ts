import { Request, Response, NextFunction } from "express";

exports.add_new_subscription = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  try {
  } catch (error) {
    next(error);
  }
};
